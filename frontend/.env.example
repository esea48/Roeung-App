# Copy to .env and fill in your values before running the frontend.

# Backend URL for the Vite dev-server proxy. Leave as-is for local development.
VITE_API_PROXY_TARGET=http://localhost:8000

# Supabase project URL — used by keeperClient.js for Keeper email/password auth.
# Find in: Supabase dashboard > Project Settings > API > Project URL
VITE_SUPABASE_URL=

# Supabase anon/public key — safe to expose in the browser.
# Find in: Supabase dashboard > Project Settings > API > anon public
VITE_SUPABASE_ANON_KEY=

# Browser smoke test configuration.
SMOKE_WEB_BASE_URL=http://localhost:5173
SMOKE_FAMILY_ACCESS_TOKEN=replace-with-family-access-token
SMOKE_KEEPER_EMAIL=replace-with-keeper-email
SMOKE_KEEPER_PASSWORD=replace-with-keeper-password
SMOKE_AUDIO_PATH=/absolute/path/to/audio-fixture.wav
SMOKE_CHROME_PATH=/Applications/Google Chrome.app/Contents/MacOS/Google Chrome
SMOKE_TIMEOUT_SECONDS=900
